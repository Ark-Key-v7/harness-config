---
spec_domain: "TEMPLATE_VALUE_REQUIRED — kebab-case, e.g. billing"
status: living
derived_from: "TEMPLATE_VALUE_REQUIRED — first archived change, e.g. specs/changes/archive/2026-10-01-add-billing"
last_reconciled: "TEMPLATE_VALUE_REQUIRED — date or SHA of last archive merge"
---

# Domain Spec: <domain>

**What this file is.** The living source of truth for what the <domain>
capability DOES NOW. It changes only through the archive merge of a change
package's delta.md — never by hand. Why any requirement exists: follow the
requirement's provenance line to its archived change, then to the Phase-0
chain (intent → prd → plan).

## Requirements

<!-- Merged content. Each requirement block:
### REQ-<domain>-<nnn>: <name>
<requirement text — one verifiable statement>
provenance: specs/changes/archive/<date>-<slug>

#### Scenario: <name>
- **GIVEN** <precondition>
- **WHEN** <action>
- **THEN** <observable outcome>
-->
TEMPLATE_VALUE_REQUIRED
