# Validation Gates

This directory contains smoke tests, fire tests, and acceptance gates for the
Factory Rig.

A component may move from `planned` to `installed_validated` only after its
command-level validation succeeds and produces the expected evidence.
