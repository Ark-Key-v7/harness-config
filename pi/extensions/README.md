# Baseline Extension Set

Required baseline extensions:

1. Bash guard
2. File-changes log with undo
3. Ask-user tool
4. Memory toggle

These slots are intentionally inert until the pinned Pi extension API is
inspected. Do not fabricate extension source against an unverified API.
